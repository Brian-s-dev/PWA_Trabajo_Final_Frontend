import React from 'react';

const VerifyEmailScreen = () => {
    return (
        <div>
            <h1>Verificando Correo...</h1>
            <p>Pantalla que capturará el token de la URL.</p>
        </div>
    );
};

export default VerifyEmailScreen;
